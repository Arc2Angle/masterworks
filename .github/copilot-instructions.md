Read the relevant docs sub directories, which provide detailed information about the main tools used by the project.

Instead of using `grep` or `find`, list directory contents or read `index.md` files to understand the structure and purpose of each directory.